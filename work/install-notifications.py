from pathlib import Path
p=Path('work/partant.template.html');s=p.read_text()
s=s.replace("bindAccount(identity.accounts.find(a=>a.id===identity.current));", "identity.events??=[];identity.eventSequence??=0;identity.eventSnapshots??=Object.fromEntries(state.bookings.map(b=>[b.id,bookingEventSnapshot(b)]));\nbindAccount(identity.accounts.find(a=>a.id===identity.current));")
s=s.replace("function persist(){if(identity){customer.favorites", "function persist(){if(identity){captureBookingEvents();customer.favorites")
s=s.replace('let special={accounts:accountsPage,conversations:conversationsPage};', 'let special={accounts:accountsPage,conversations:conversationsPage,notifications:notificationsPage};')
s=s.replace("if(state.screen==='conversations'&&state.auth?.role==='coach')", "if(['conversations','notifications'].includes(state.screen)&&state.auth?.role==='coach')")
s=s.replace('>Changer de compte</button><button class="textbtn" style="color:inherit" data-action="conversations">', '>Démo · Comptes</button><button class="textbtn" style="color:inherit" data-action="conversations">')
s=s.replace("Messages${unread?' · '+unread:''}</button></div><h1>", "Messages${unread?' · '+unread:''}</button>${notificationButton()}</div><h1>")
s=s.replace('`<div class="row between space"><button class="textbtn" style="color:inherit" data-action="accounts">', '`<div class="row between wrap space"><button class="textbtn" style="color:inherit" data-action="accounts">')
s=s.replace("if(state.screen==='profile'&&state.auth?.role==='coach'", "if(state.screen==='coach'&&state.coachTab==='agenda')html=html.replace('<div class=\"sheet section\">','<div class=\"sheet section\">'+notificationBanner());if(state.screen==='account')html=html.replace('<button class=\"setting-row\" data-action=\"conversations\">','<button class=\"setting-row\" data-action=\"notifications\"><span>Mes notifications'+(unreadNotifications().length?' · '+unreadNotifications().length:'')+'</span>'+ic('chevron')+'</button><button class=\"setting-row\" data-action=\"conversations\">');if(state.screen==='profile'&&state.auth?.role==='coach'")
s=s.replace("switch(a){case 'accounts':", "switch(a){case 'notifications':go('notifications');return;case 'notifications-read':myNotifications().forEach(n=>n.read=true);persist();render();return;case 'notification-open':{let n=myNotifications().find(n=>n.id===d.id),b=n&&bookingRecord(n.bookingId);if(!b||!canReadBooking(b)){toast('Cette séance n’est plus accessible.');return}n.read=true;persist();if(state.auth?.role==='coach'){state.coachDay=b.day;prior_act('coach-session',{id:b.id})}else{state.selectedBooking=b.id;go('detail')}return}case 'accounts':")
s=s.replace("function resetIdentityDemo(){identity.messages={};", "function resetIdentityDemo(){identity.events=[];identity.eventSnapshots={};identity.eventSequence=0;identity.messages={};")
s=s.replace('Préférences enregistrées localement. Aucune notification réelle n’est envoyée.', 'Les nouvelles réservations et les changements activent les compteurs et alertes dans l’agenda. Leur historique reste consultable dans Notifications. Rappels et actualités : préférences de démonstration, sans envoi externe.')
s=s.replace('\nsyncAllCoaches();\n\nrender();', '\n'+Path('work/booking-notifications.js').read_text()+'\nsyncAllCoaches();\n\nrender();')
p.write_text(s)
